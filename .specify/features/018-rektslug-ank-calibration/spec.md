# Feature Specification: Rektslug-Ank Calibration

**Feature ID**: 018
**Canonical Source**: `specs/018-rektslug-ank-calibration/spec.md`

This mirror exists for `.specify` compatibility.

The source of truth remains:
- `specs/018-rektslug-ank-calibration/spec.md`

The implementation goal is unchanged:
- calibrate local `liq-map` output specifically against CoinAnk
- keep BTC/ETH and `1d/1w` scope
- preserve a separate default local profile
