# Feature Specification: Provider Liq-Heat-Map Comparison

**Feature ID**: 018
**Canonical Source**: `specs/018-liqheatmap-provider-comparison/spec.md`

This mirror exists for `.specify` compatibility.

The source of truth remains:
- `specs/018-liqheatmap-provider-comparison/spec.md`

The implementation goal is unchanged:
- compare `liq-heat-map` only
- compare local vs CoinAnk vs Coinglass
- support only `BTC/ETH`
- support only `48h/7d`
- exclude `liq-map` from this spec
