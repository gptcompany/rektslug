# Feature Specification: Provider Liq-Map Comparison

**Feature ID**: 017
**Canonical Source**: `specs/017-liqmap-provider-comparison/spec.md`

This mirror exists for `.specify` compatibility.

The source of truth remains:
- `specs/017-liqmap-provider-comparison/spec.md`

The implementation goal is unchanged:
- compare `liq-map` only
- compare local vs CoinAnk vs Coinglass
- support only `BTC/ETH`
- support only `1d/1w`
- exclude `liq-heat-map` / `LiquidityHeatmap`
